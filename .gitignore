_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea
.venv

# Local editor backups — must never be committed: files with front matter are
# rendered by Jekyll and collide with the pages they back up.
*.backupclaude*
*.bak

# Claude Code local config and session artifacts
.claude/
CLAUDE.md
